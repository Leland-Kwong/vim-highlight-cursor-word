# Vim Highlight Cursor Word

Highlights the word under the cursor on `CursorHold` after X milliseconds

## Options

```vim
" change highlight delay
let g:highlightWordDelay = 400
" change highlight style
hi WordUnderCursor guifg=#333
```
